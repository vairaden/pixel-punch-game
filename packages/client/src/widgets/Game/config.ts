const HERO = {
  startX: 60,
  startY: 60,
  radius: 20,
  speed: 5,
  health: 100,
  damage: 10,
};

const BASE = {
  width: 80,
  height: 80,
  health: 750,
};

const ENEMY = {
  radius: 20,
  speed: 0.5,
  health: 25,
  damage: 4,
  attackInterval: 500,
};
export const config = {
  HERO,
  BASE,
  ENEMY,
};
